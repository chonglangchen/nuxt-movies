# https://developers.themoviedb.org/3/getting-started/introduction
TMDB_API_KEY=

# Optional: HTTP proxy for bypassing network restrictions (e.g. http://127.0.0.1:7890)
# Leave empty if you can access TMDB directly (e.g. in CI or outside restricted regions)
HTTP_PROXY=
